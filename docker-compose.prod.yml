version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    restart: always
    environment:
      - ADMIN_TOKEN=${ADMIN_TOKEN}
    # In production you likely want to disable mounting code and use volumes carefully
